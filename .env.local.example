# Supabase
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Stripe
STRIPE_SECRET_KEY=sk_test_...
STRIPE_WEBHOOK_SECRET=whsec_...
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_...

# Bunny Stream
BUNNY_TOKEN_KEY=your-bunny-token-key
BUNNY_API_KEY=your-bunny-api-key
BUNNY_LIBRARY_ID=your-library-id

# Bunny Storage — HTG2 recording zone (primary storage for all live session recordings)
#
# HTG2 serves audio-only recordings (sesja phase) and internal admin video (wstep/podsumowanie).
# We use Bunny Storage (raw files) instead of Bunny Stream (HLS transcoding) because:
#   - We don't stream video — only audio playback for clients
#   - Stream transcodes into multi-bitrate HLS variants → 10x storage cost
#   - Storage keeps the raw file as-is → 1x cost
#
# Upload path (cron writes): R2 → Bunny Storage zone "htg-backup-sessions"
# Playback path (token endpoint reads): client → HTG2 Pull Zone → Bunny Storage
#
# ── Setup in bunny.net panel ───────────────────────────────────────────────
# 1. Storage → Storage Zones → Add Storage Zone
#    - Name: "htg-backup-sessions" (or similar; must match BUNNY_BACKUP_STORAGE_ZONE)
#    - Region: pick closest to your users
# 2. Open the zone → FTP & API Access → copy the "Password"
#    (zone-specific API key, NOT account-level)
# 3. CDN → Pull Zones → Add Pull Zone
#    - Origin: the storage zone you created above
#    - Name: "htg-backup-sessions" → gives you https://htg-backup-sessions.b-cdn.net
# 4. Open the Pull Zone → Security → Enable Token Authentication
#    - Copy the "Authentication Key" → this goes into BUNNY_HTG2_CDN_TOKEN_KEY
#
# Write side (cron upload):
BUNNY_BACKUP_STORAGE_ZONE=htg-backup-sessions
BUNNY_BACKUP_STORAGE_API_KEY=
BUNNY_BACKUP_STORAGE_HOSTNAME=storage.bunnycdn.com

# Read side (token endpoint playback URL signing):
BUNNY_HTG2_CDN_URL=https://htg-backup-sessions.b-cdn.net
BUNNY_HTG2_CDN_TOKEN_KEY=

# LiveKit Cloud (WebRTC + egress for HTG Meetings and Live Sessions)
LIVEKIT_URL=wss://your-livekit-instance.livekit.cloud
LIVEKIT_API_KEY=your-livekit-api-key
LIVEKIT_API_SECRET=your-livekit-api-secret

# Cloudflare R2 (LiveKit egress sink — MP4 files before upload to Bunny)
R2_ACCESS_KEY=your-r2-access-key
R2_SECRET_KEY=your-r2-secret-key
R2_BUCKET=htg-rec
R2_ENDPOINT=https://your-account.r2.cloudflarestorage.com

# OpenAI Whisper (AI transcription for HTG Meeting per-speaker tracks, PR #10)
# Also used by scripts/review.ts (reviewer 2: o4-mini / o3)
OPENAI_API_KEY=sk-...

# AI Code Review (scripts/review.ts)
ANTHROPIC_API_KEY=sk-ant-...
GEMINI_API_KEY=AIza...

# HTG Meetings — storage path salt for privacy hash
# Deterministic SHA-256(user_id + salt) hash used in Bunny Storage paths
# instead of raw user_id — admin without salt cannot correlate paths to users.
# REQUIRED in all environments (dev/staging/prod). Set any random string locally.
MEETING_PATH_SALT=dev-salt-change-me

# Cron endpoints — shared secret for Vercel Cron Bearer auth.
# REQUIRED in production. Cron endpoints (process-recordings, orphan-reaper)
# reject requests without matching Authorization: Bearer ${CRON_SECRET}.
CRON_SECRET=

# WebAuthn / Passkey
WEBAUTHN_RP_ID=localhost
WEBAUTHN_ORIGIN=http://localhost:3000
WEBAUTHN_CHALLENGE_SECRET=dev-secret-change-in-production

# Admin
ADMIN_EMAILS=natalia@htg.cyou

# Sentry — error observability (rate-limit + general server errors)
#
# Optional in dev (Sentry.init becomes a no-op if DSN is missing).
# REQUIRED in production for rate-limit alerts to work.
#
# Setup:
#   1. sentry.io → Create Project → "Next.js" → copy DSN
#   2. Vercel project settings → Environment Variables → add NEXT_PUBLIC_SENTRY_DSN
#   3. (Optional) Add SENTRY_AUTH_TOKEN for source maps upload
#
# Suggested alerts:
#   - Pattern: "[rate-limit] check threw" OR "check failed" → threshold >5/min
#     (security degradation = limiter wyłączony globalnie)
#   - Pattern: "[rate-limit] log insert failed" → threshold >10/min
#     (lokalny bypass limitu — możliwe enumeration attack)
NEXT_PUBLIC_SENTRY_DSN=

# YouTube playlist banner — per-locale playlists for /konto banner.
# Empty = fallback to PL. Cron checks every 6h for new videos.
YOUTUBE_PLAYLIST_PL=PL0VSN6lmYC8PDaFoeeurHSrwy8u3cNNHd
YOUTUBE_PLAYLIST_EN=
YOUTUBE_PLAYLIST_DE=
YOUTUBE_PLAYLIST_PT=

# Zoom Server-to-Server OAuth (used by scripts/zoom-export-to-bunny.ts)
#
# Setup: marketplace.zoom.us → Develop → Build App → Server-to-Server OAuth
# Required scopes:
#   cloud_recording:read:list_account_recordings:admin
#   cloud_recording:read:recording:admin
#   user:read:list_users:admin
ZOOM_ACCOUNT_ID=
ZOOM_CLIENT_ID=
ZOOM_CLIENT_SECRET=

# Zoom Server-to-Server OAuth — osobna aplikacja dla scripts/zoom-download-local.ts
# (np. konto natalia@htg.cyou). Skrypt najpierw szuka ZOOM_DOWNLOAD_*, w razie
# braku robi fallback na ZOOM_* powyżej. Te same scopes co wyżej.
ZOOM_DOWNLOAD_ACCOUNT_ID=
ZOOM_DOWNLOAD_CLIENT_ID=
ZOOM_DOWNLOAD_CLIENT_SECRET=
